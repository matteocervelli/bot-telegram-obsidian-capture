services:
  bot:
    build: .
    container_name: telegram-obsidian-bot
    restart: unless-stopped
    env_file: .env
    environment:
      - VAULT_PATH=/vault
    volumes:
      - ${VAULT_PATH}:/vault
